# Local development environment variables.
# This repo is public. Never commit real secrets to git.

# Gemini (required for real enrichment)
GEMINI_API_KEY=
# Example values: gemini-2.0-flash, gemini-2.0-pro (pick based on your account/quota)
GEMINI_MODEL=

# Foundry (required only when running in Foundry/pipeline mode)
FOUNDRY_URL=

# Optional tuning knobs (add support in code as it lands)
WORKERS=
MAX_RETRIES=
REQUEST_TIMEOUT=
FAIL_FAST=

