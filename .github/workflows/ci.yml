name: ci

on:
  pull_request:
  push:
    branches:
      - main
    tags:
      - "v*"

permissions:
  contents: read
  packages: write

jobs:
  verify:
    runs-on: blacksmith-2vcpu-ubuntu-2404
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Cache godel distribution
        uses: actions/cache@v4
        with:
          path: ~/.godel
          key: godel-${{ runner.os }}-${{ hashFiles('godelw', 'godel/config/godel.properties') }}

      - name: Verify
        run: ./godelw verify --apply=false

      - name: Verify public package consumers
        run: bash ./test/scripts/verify_public_modules.sh

      - name: Verify dev CLI behavior
        run: bash ./test/scripts/dev_cli_test.sh

      - name: Build linux/amd64 binary
        run: |
          mkdir -p out
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -trimpath -ldflags="-s -w" -o out/enricher_linux_amd64 ./cmd/enricher

      - name: Upload binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: enricher_linux_amd64
          path: out/enricher_linux_amd64

  gemini-e2e:
    runs-on: blacksmith-2vcpu-ubuntu-2404
    needs: verify
    steps:
      - name: Check Gemini secrets
        id: gemini_secrets
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GEMINI_MODEL: ${{ secrets.GEMINI_MODEL }}
        run: |
          set -euo pipefail
          enabled="true"
          if [[ -z "${GEMINI_API_KEY}" || -z "${GEMINI_MODEL}" ]]; then
            enabled="false"
            echo "Gemini secrets not configured; skipping gemini-e2e job." >&2
          fi
          echo "enabled=${enabled}" >> "${GITHUB_OUTPUT}"

      - uses: actions/checkout@v4
        if: ${{ steps.gemini_secrets.outputs.enabled == 'true' }}

      - uses: actions/setup-go@v5
        if: ${{ steps.gemini_secrets.outputs.enabled == 'true' }}
        with:
          go-version-file: go.mod
          cache: true

      - name: E2E (real Gemini)
        if: ${{ steps.gemini_secrets.outputs.enabled == 'true' }}
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GEMINI_MODEL: ${{ secrets.GEMINI_MODEL }}
          GEMINI_CAPTURE_AUDIT: "true"
          # go test executes each package with a package-level working dir; use an absolute
          # path so CSV artifacts land in a stable location for upload.
          GEMINI_E2E_ARTIFACT_DIR: "${{ github.workspace }}/out/gemini-e2e"
        run: |
          set -euo pipefail
          go test -tags=gemini_e2e ./...

      - name: Upload gemini e2e CSVs
        if: ${{ always() && steps.gemini_secrets.outputs.enabled == 'true' }}
        uses: actions/upload-artifact@v4
        with:
          name: gemini-e2e-csv
          path: out/gemini-e2e/*.csv
          if-no-files-found: warn

  docker-compose-e2e:
    runs-on: blacksmith-2vcpu-ubuntu-2404
    needs: verify
    steps:
      - name: Check Gemini secrets
        id: gemini_secrets
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GEMINI_MODEL: ${{ secrets.GEMINI_MODEL }}
        run: |
          set -euo pipefail
          enabled="true"
          if [[ -z "${GEMINI_API_KEY}" || -z "${GEMINI_MODEL}" ]]; then
            enabled="false"
            echo "Gemini secrets not configured; skipping docker-compose-e2e job." >&2
          fi
          echo "enabled=${enabled}" >> "${GITHUB_OUTPUT}"

      - uses: actions/checkout@v4
        if: ${{ steps.gemini_secrets.outputs.enabled == 'true' }}

      - name: Install Venom
        if: ${{ steps.gemini_secrets.outputs.enabled == 'true' }}
        run: |
          set -euo pipefail
          VENOM_VERSION="v1.3.0"
          VENOM_SHA256="89832ec25e820c605cf0d3c09122e60bad43d13c1724aa6d375ef7109fbfe201"
          curl -fsSL -o /tmp/venom "https://github.com/ovh/venom/releases/download/${VENOM_VERSION}/venom.linux-amd64"
          echo "${VENOM_SHA256}  /tmp/venom" | sha256sum -c -
          install -m 0755 /tmp/venom /usr/local/bin/venom
          venom version

      - name: E2E (docker compose + venom, real Gemini)
        if: ${{ steps.gemini_secrets.outputs.enabled == 'true' }}
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GEMINI_MODEL: ${{ secrets.GEMINI_MODEL }}
          GEMINI_CAPTURE_AUDIT: "true"
        run: |
          set -euo pipefail
          mkdir -p out/venom
          cd out/venom
          venom run ../../test/venom/enricher_e2e.yml --output-dir . --format=xml --html-report -v

      - name: Docker Compose Down (Always)
        if: ${{ always() && steps.gemini_secrets.outputs.enabled == 'true' }}
        run: |
          set -euo pipefail
          docker compose -f docker-compose.test.yml down -v --remove-orphans || true

      - name: Upload docker compose e2e artifacts
        if: ${{ always() && steps.gemini_secrets.outputs.enabled == 'true' }}
        uses: actions/upload-artifact@v4
        with:
          name: docker-compose-e2e
          path: |
            out/venom/*
            test/artifacts/mock-foundry/uploads/**
          if-no-files-found: warn

  docker:
    runs-on: blacksmith-4vcpu-ubuntu-2404
    needs: verify
    steps:
      - uses: actions/checkout@v4

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=sha,format=long
            type=ref,event=tag
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Setup Blacksmith Builder
        uses: useblacksmith/setup-docker-builder@v1

      - name: Login to GHCR
        if: github.event_name == 'push'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build (and maybe push)
        uses: useblacksmith/build-push-action@v2
        with:
          context: .
          file: Dockerfile
          platforms: linux/amd64
          push: ${{ github.event_name == 'push' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
