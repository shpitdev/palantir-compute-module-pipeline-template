package processor

import (
	"context"
	"strings"
)

type Result struct {
	Input string
	Value string
}

type Processor struct{}

func (Processor) Process(_ context.Context, in string) (Result, error) {
	trimmed := strings.TrimSpace(in)
	return Result{Input: in, Value: strings.ToUpper(trimmed)}, nil
}
