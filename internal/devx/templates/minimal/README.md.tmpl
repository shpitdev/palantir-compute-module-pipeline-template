# {{.Name}}

A minimal Go compute-module starter generated by `foundry-cmgo`.

## Local development

```bash
go test ./...
go run ./cmd/compute-module
```

Edit `processor/processor.go` first. Keep transform logic isolated there so local tests and future Foundry runtime wiring can exercise the same code path.

## Foundry/local emulation inputs

Use `foundry-cmgo seed dataset` from the template repo to copy a CSV into the mock Foundry input layout:

```bash
foundry-cmgo seed dataset --csv ./input.csv --alias-map ./test/fixtures/alias-map.json --alias input
```

Use `foundry-cmgo seed stream` when a running mock Foundry server has the target RID registered as a stream:

```bash
foundry-cmgo seed stream --csv ./records.csv --alias-map ./test/fixtures/alias-map.json --alias output --url http://localhost:8080
```
