package processor

import (
	"context"
	"testing"
)

func TestProcessor(t *testing.T) {
	t.Parallel()

	out, err := (Processor{}).Process(context.Background(), " alice@example.com ")
	if err != nil {
		t.Fatalf("Process failed: %v", err)
	}
	if out.Value != "ALICE@EXAMPLE.COM" {
		t.Fatalf("unexpected value: %q", out.Value)
	}
}
